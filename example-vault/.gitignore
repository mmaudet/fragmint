# Outputs are generated by composition API — no need to version
outputs/

# SQLite database (runtime state, not versioned)
.fragmint.db
.fragmint.db-shm
.fragmint.db-wal
.fragmint.db-journal

# OS files
.DS_Store
