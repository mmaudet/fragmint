node_modules/
dist/
*.db
*.sqlite
.env
.env.local
.env.*.local
.DS_Store
data/
# example-vault is managed by fragmint (has its own .git)
# Only track the .gitignore and .gitkeep files in the main repo
example-vault/fragments/
example-vault/outputs/
example-vault/.git/
example-vault/.DS_Store
packages/web/tsconfig.tsbuildinfo
.superpowers/
docs/superpowers/
# Ignore all Claude Code configuration (local tooling, not project code)
.claude/
*.~lock.*
e2e/demo/output/

# Mission planning documents (local only)
.claude/mission/

# SQLite WAL files
*.db-shm
*.db-wal
*.db-journal
# Personal notes and learning references (local only)
.claude/notes/
